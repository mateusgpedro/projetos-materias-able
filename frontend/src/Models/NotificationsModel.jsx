export default class NotificationModel {
    constructor(title, message, sender, time, url) {
        this.title = title;
        this.message = message;
        this.sender = sender;
        this.time = time;
        this.url = url;

    }
}